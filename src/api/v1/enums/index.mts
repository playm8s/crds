'use strict';

export enum Games {
  csgo = 'csgo',
  cs2 = 'cs2',
}

export enum StorageStrategies {
  raw = 'raw',
  overlayfs = 'overlayfs',
}

export enum StatusReasons {
  created = 'created',
  updated = 'updated',
  deleted = 'deleted',
  modified = 'modified',
}
