name: Release

on:
  push:
    tags:
      - '*'
      - '!*-build'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: crds
  cancel-in-progress: false

defaults:
  run:
    shell: bash

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: "${{ secrets.GH_PAT }}"
          clean: true
          fetch-depth: 20

      - name: Setup nodejs
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: "https://npm.pkg.github.com"

      - name: NPM publish
        shell: bash
        run: |
          cat <<EOF >> $HOME/.npmrc
          //npm.pkg.github.com/:_authToken=${GH_PAT}
          @playm8s:registry=https://npm.pkg.github.com/
          @thehonker:registry=https://npm.pkg.github.com/
          EOF
          npm install --include=dev --legacy-peer-deps
          npm publish
        env:
          GH_PAT: ${{ secrets.GH_PAT }}

      - name: Create tarball of crds
        shell: bash
        run: |
          tar -cvf crds.tar crds/

      - name: Create Github Release
        uses: softprops/action-gh-release@v2
        with:
          make_latest: true
          generate_release_notes: true
          files: |
            crds.tar
            crds/*

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Extract metadata (tags, labels) for Docker (CRD Job image build)
        id: docker_meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest

      - name: Build and Push CRD Job Image
        uses: docker/bake-action@v6
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          push: true
          source: .
          files: |
            ./docker-bake.hcl
            cwd://${{ steps.docker_meta.outputs.bake-file }}
          targets: build
